using UnityEngine;

public class fjendescript : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("ICH BIN HITLER!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
